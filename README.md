Burmont Cleaning Co. mobile menu fix

This update fixes the mobile dropdown menu.

Cause:
A previous header fix used overflow clipping on the sticky header to hide a rogue line. That clipped the mobile dropdown menu, so the button looked like it stopped working.

Files to upload to repo root:
- index.html
- styles.css
- script.js
- README.md
